# Node.js dependencies (Dit is de boosdoener van de grootte!)
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
package-lock.json # (Optioneel: verwijder deze regel als je wil dat anderen exact dezelfde pakketversies gebruiken)

# Electron build en export mappen (.exe, .dmg, etc.)
dist/
dist-zip/
out/
build/
release/

# Lokale omgevingsvariabelen (Voor je eigen veiligheid)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Besturingssysteem log- en cachebestanden
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Code editor mappen (VS Code, IntelliJ, etc.)
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj

# settings die niet moeten worden meegenomen
character_settings.json

# temp backend ignore until fixed later
Backend/